// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "vim_mode == normal && !menu",
    "bindings": {
      "space f f": "file_finder::Toggle",
      "space b": "workspace::NewFile",
      "space x": "workspace::CloseWindow",
      "space /": "vim::ToggleComments"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "ctrl-n": "workspace::ToggleRightDock",
      "tab": "workspace::ActivateNextPane", // this doesn't work for some reason...
      "shift-tab": "workspace::ActivatePreviousPane",
      "alt-h": "workspace::OpenTerminal" //also broken
    }
  },
  {
    "context": "Editor",
    "bindings": {
      // "j k": ["workspace::SendKeystrokes", "escape"]
    }
  }
]
